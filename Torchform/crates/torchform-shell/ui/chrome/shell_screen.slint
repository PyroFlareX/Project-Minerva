// =============================================================================
// chrome/shell_screen.slint — top-level screen route enum
//
// Mirrors `S.screen` in torchform-os.html. Panels (Quick Settings, Notifications,
// Switcher) are tracked separately as visibility flags on the window.
// =============================================================================

export enum ShellScreen {
    Lock,
    Home,
    App,
}
