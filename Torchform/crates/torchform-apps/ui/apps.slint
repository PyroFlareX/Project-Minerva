// =============================================================================
// torchform-apps/ui/apps.slint — re-export hub for all 12 app components
// =============================================================================

import { AppSettings, SettingsEntry }     from "app_settings.slint";
import { AppFiles, FileEntry }             from "app_files.slint";
import { AppFileManager }                  from "app_file_manager.slint";
import { AppCamera }                       from "app_camera.slint";
import { AppWebBrowser }                   from "app_web_browser.slint";
import { AppMediaPlayer }                  from "app_media_player.slint";
import { AppNetworkManager, WifiNetwork, BtDevice } from "app_network_manager.slint";
import { AppTerminal }                     from "app_terminal.slint";
import { AppPhone }                        from "app_phone.slint";
import { AppSms, SmsThread, SmsMessage }   from "app_sms.slint";
import { AppEmail, EmailMsg }              from "app_email.slint";
import { AppSysmon }                       from "app_sysmon.slint";
import { AppPkgman, PkgEntry }             from "app_pkgman.slint";
import { AppLogview }                      from "app_logview.slint";
import { AppNotes, NoteEntry }             from "app_notes.slint";

export {
    AppSettings, SettingsEntry,
    AppFiles, FileEntry,
    AppFileManager,
    AppCamera,
    AppWebBrowser,
    AppMediaPlayer,
    AppNetworkManager, WifiNetwork, BtDevice,
    AppTerminal,
    AppPhone,
    AppSms, SmsThread, SmsMessage,
    AppEmail, EmailMsg,
    AppSysmon,
    AppPkgman, PkgEntry,
    AppLogview,
    AppNotes, NoteEntry,
}
